<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0
                             http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>ai.lingshu</groupId>
        <artifactId>lingshu-parent</artifactId>
        <version>${revision}</version>
    </parent>

    <artifactId>lingshu-examples</artifactId>
    <name>LingShu Examples (parent)</name>
    <description>Examples aggregator — ≤10 examples per Story, each ≤ 100 lines (dsh §10.2)</description>

    <packaging>pom</packaging>

    <modules>
        <module>demo-empty</module>
        <module>demo-engineer</module>
        <module>demo-local-tools</module>
        <!-- 🆕 Stage A skeletons (Story #003/#004/#005/#006/#007/#008/#009[a-e]/#018/#020[a-c]/#021[a-c]/#022/#023/#024) -->
        <module>demo-spi</module>
        <module>demo-parallel</module>
        <module>demo-cancellation</module>
        <module>demo-tenants</module>
        <module>demo-reload</module>
        <module>demo-max-steps</module>
        <module>demo-compactor</module>
        <module>demo-a2a</module>
        <module>demo-skill</module>
        <!-- 🆕 Story #021a + #021b + #021c — McpServerConnection 3 transport + Tool adapter + Factory dispatch -->
        <module>demo-mcp</module>
        <!-- 🆕 Story #022 + #023 + #024 — @AgentTool + DelegateTool + [TOOL SCHEMAS] 段 wiring -->
        <module>demo-delegate</module>
        <!-- 🆕 Story #025 — demo-product comprehensive HTTP SSE chat product -->
        <module>demo-product</module>
        <!-- 🆕 Story #025b — demo-product-a2a-server, custom A2A server with translate skill (pairs with demo-product) -->
        <module>demo-product-a2a-server</module>
    </modules>

    <dependencies>
        <dependency>
            <groupId>ai.lingshu</groupId>
            <artifactId>lingshu-core</artifactId>
        </dependency>
    </dependencies>
</project>