# Requirements Checklist — Story #004 tool-parallel-dispatch

> Generated by `/speckit-checklist` against `spec.md` (3 US + 9 Edge Cases + 10 FR + 5 NFR + 6 SC).
> Verify each item before PR sign-off.

---

## Spec Quality

- [x] **WHY/WHO/WHAT** captured: 3 user stories (Alice / Charlie) + AC-03 explicit
- [x] **All AC mapped**: AC-03 → US2 (sole AC for Story #004)
- [x] **Priority labels**: all 3 US are P1 (engine without tool execution = toy)
- [x] **Independent Tests** per US: each US has a self-contained black-box test
- [x] **Acceptance Scenarios** use Given/When/Then with concrete numbers (1.3s / 3.0×)
- [x] **Edge Cases**: 9 enumerated (timeout, AskUser, empty list, single tool, NPE, parallelism boundaries, missing bean, etc.)
- [x] **Functional Requirements**: 10 numbered, each tied to a US
- [x] **Non-Functional Requirements**: 5 (perf + JDK 8 + thread naming + zero-dep + ErrorCode reuse)
- [x] **Key Entities**: 7 (LinearTurnEngine / Provider / Config / ToolExecutor / 3 test classes)
- [x] **Success Criteria**: 6 measurable (SC-001—SC-006)

## Design Doc Cross-Reference

- [x] dsh §6.1 LinearTurnEngine dispatchParallel algorithm — reflected in `contracts/parallel-dispatch-pipeline.md`
- [x] dsh §4.6 ToolExecutor 5-step pipeline — preserved (not duplicated by engine)
- [x] dsh §4.7 PermissionPolicy 3 decisions (Allow / Deny / AskUser) — handled in `dispatchWithPolicy`
- [x] dsh §14.7 shared thread pool — `agentToolPool` Bean
- [x] dsh §15 ErrorCode LINGS-T02 / T04 / C02 — reused (no new codes)

## Constitution Compliance

- [x] §1 #7 编排可扩展 — FlowEngine interface unchanged
- [x] §1 #11 默认实现位置 — ToolExecutorConfig in `lingshu-core`
- [x] §2 13 项依赖锁定 — 0 new dependencies (verified via `dependency:tree` diff)
- [x] §3 NFR — AC-03 wall-clock 1.3s aligns with turn P50 ≤ 30s budget
- [x] §4 LINGS-Txx — Tool domain reused, no new error codes
- [x] §5 7 层金字塔 — Tests span L1 (6) + L2 (5) + L2P (4) + E2E (1) = 16 cases
- [x] §10 R-13 — Spring AI transitive pollution guarded via dep-tree self-check (T037)

## Story Boundary (CLAUDE.md §11 #4)

- [x] **≤ 5 核心文件改动**: 5 modified (LinearTurnEngine / Provider / DefaultToolExecutor / AgentFactory-not-needed / ToolExecutorConfig-new)
- [x] **≤ 3 ErrorCode 引入**: 0 new (LINGS-T02/T04/C02 reused)
- [x] **≤ 5 测试文件**: 4 new test classes (within limit)
- [x] No cross-Story scope creep (AskUser → #005, sandbox → #016, timeout wrap → #011)

## Test Coverage

- [x] L1-001 to L1-006 — DefaultToolExecutor translation (6 cases)
- [x] L2-001 to L2-005 — LinearTurnEngine sequential dispatch (5 cases)
- [x] L2P-001 to L2P-004 — Parallel dispatch AC-03 (4 cases, **includes AC-03 main path**)
- [x] E2E-001 — End-to-end smoke (1 case)
- [x] Total: 16 cases across 4 test classes

## Risk Mitigation

- [x] R-01 OOM risk — bounded thread pool + CallerRunsPolicy
- [x] R-02 future leak — every future has get(timeout); daemon pool
- [x] R-03 deadlock — Semaphore-bound concurrency, no tool→tool join
- [x] R-04 flaky CI — CountDownLatch synchronization + 1.5s margin
- [x] R-05 dep pollution — R-13 mandatory dep-tree check

## Definition of Done (Plan.md)

- [ ] All 10 implementation phases complete (T001—T039)
- [ ] PR opened with title `feat(agent): Story #004 tool-parallel-dispatch — ...`
- [ ] PR body includes AC-03 output + R-13 dep-tree section
- [ ] Critical invariants list all preserved
- [ ] No new Maven dependency

## Anti-Patterns (from tasks.md)

- [x] Did NOT call `tool.execute()` directly (only via `toolExecutor.dispatch()`)
- [x] Did NOT use Spring AI auto-execution
- [x] Did NOT use `ExecutorCompletionService` / `ForkJoinPool` (JDK 8 deps)
- [x] Did NOT modify Tool / ToolExecutor / PermissionPolicy / Decision signatures
- [x] Did NOT use `var` / `List.of` / sealed interface (JDK 8 constraint)
- [x] Did NOT throw RuntimeException from runTurn (all translated to ToolResult.error)
- [x] Did NOT include AskUser / ApprovalGate full impl (Story #005 scope)

## Reviewer Sign-Off Checklist

When reviewing this Story's PR, verify:

1. **AC-03 black-box output** is pasted into PR body — wall-clock ≤ 1.3s
2. **Speedup ratio** calculation shown — ≥ 3.0×
3. **`mvn dependency:tree` diff** = 0
4. **No regression** in Story #001—#003 tests
5. **Critical invariants** section is complete + accurate
6. **DefaultToolExecutor** still preserves 5-step pipeline order (only added outer try-catch)
7. **LinearTurnEngine** final fields all wired correctly (4 fields: promptBuilder, llmProvider, toolExecutor, permissionPolicy + toolPool)
8. **LinearTurnEngineProvider** has 5 dependencies (was 2): 4 Routers + 1 ExecutorService
9. **ToolExecutorConfig** `agentToolPool` has daemon threads + lingshu-tool- prefix
10. **No new ErrorCodes** (constitution §4 boundary)

---

**Checklist Generated**: 2026-09-20
**Story #004 Status**: Ready for implementation (Step 3 in progress)
