# Requirements Checklist — Story #005 cancellation-token

> Generated by `/speckit-checklist` against `spec.md` (4 US + 9 Edge Cases + 14 FR + 8 NFR + 7 SC).
> Verify each item before PR sign-off.

---

## Spec Quality

- [x] **WHY/WHO/WHAT** captured: 4 user stories (Alice / Charlie / Bob-deferred) + AC-04 explicit
- [x] **All AC mapped**: AC-04 → US1 (sole AC for Story #005)
- [x] **Priority labels**: US1/US2/US4 are P1 (core cancellation); US3 deferred P2 (LlmProvider polling,留 Story #005b)
- [x] **Independent Tests** per US: each US has a self-contained black-box test
- [x] **Acceptance Scenarios** use Given/When/Then with concrete numbers (200ms / CANCELLED / 4 ToolResult)
- [x] **Edge Cases**: 9 enumerated (empty registry / idempotent fire / callback throws / synchronous callback / self-cancel / multi-turn / shutdown hook repeat / done+fire / pre-turn Ctrl-C)
- [x] **Functional Requirements**: 14 numbered, each tied to a US
- [x] **Non-Functional Requirements**: 8 (perf + JDK 8 + thread naming + zero-dep + ErrorCode reuse + idempotent + sync + daemon)
- [x] **Key Entities**: 8 (CancellationTokens / CancellationToken / TurnContext / DefaultTurnContext / DefaultToolExecutionContext / LinearTurnEngine / AgentFactory / DefaultAgent)
- [x] **Success Criteria**: 7 measurable (SC-001—SC-007)

## Design Doc Cross-Reference

- [x] dsh §4.6 CancellationToken interface — `isCancelled()` + `onCancel(Runnable) → Runnable` preserved; `default void fire()` added (FR-013)
- [x] dsh §14.12 N12 CancellationToken 三层贯通 — FlowEngine layer (LinearTurnEngine poll) ✓; Tool layer (DefaultToolExecutionContext cancellation) ✓; LlmProvider layer **deferred to Story #005b** (US3 P2)
- [x] dsh §6.1 LinearTurnEngine runTurn — loop head check + 200ms timeout fallback added (FR-006+FR-007)
- [x] dsh §14.6 N6 graceful shutdown — Story #005 only does in-flight turn cancel sub-set; full graceful shutdown (HealthIndicator + readiness probe) deferred to Story #013
- [x] dsh §15 ErrorCode LINGS-L02 / T06 / R01 — all reused, no new codes

## Constitution Compliance

- [x] §1 #12 启动时校验 — no new config fields; existing `turnTimeoutSeconds` reused
- [x] §1 #11 默认实现位置 — `CancellationTokens` in `lingshu-core/.../impl/concurrent/`
- [x] §2 13 项依赖锁定 — 0 new dependencies (verified via `dependency:tree` diff)
- [x] §3 NFR — AC-04 200ms cancel well within turn P99 ≤ 60s budget
- [x] §4 LINGS-L02/T06/R01 — all reused, no new error codes
- [x] §5 7 层金字塔 — Tests span L1 (9) + L2 (3) + L2P (3) + E2E (1) = 16 cases
- [x] §10 R-02 多租户 ThreadLocal 间接相关 — no ThreadLocal introduced; broadcastRegistry is plain CopyOnWriteArrayList
- [x] §10 R-13 — Spring AI transitive pollution guarded via dep-tree self-check (T026)

## Story Boundary (CLAUDE.md §11 #4)

- [x] **≤ 5 核心文件改动**: ⚠️ **轻度超** — 1 new + 6 modified = **7 files** (vs soft limit 5)
- [x] **Justification**: spec.md "超上限原因" 段 + plan.md D-01—D-08 explains 三层贯通的最小集
- [x] **Mitigation**: US3 LlmProvider cancellation deferred to Story #005b; AgentEvent unchanged (StopReason.CANCELLED reused); DefaultAgent buildContext change is 1-line
- [x] **≤ 3 ErrorCode 引入**: 0 new (LINGS-L02/T06/R01 reused)
- [x] **≤ 5 测试文件**: 5 new test classes (at limit)
- [x] No cross-Story scope creep (LlmProvider → #005b; Sandbox → #016; SessionStore flush → #014; graceful shutdown → #013)

## Test Coverage

- [x] L1-001 to L1-006 — CancellationTokens impl (6 cases)
- [x] L1-007 to L1-009 — Context sharing (3 cases)
- [x] L2-001 to L2-003 — LinearTurnEngine cancellation (3 cases, **L2-001 is AC-04 main path**)
- [x] L2P-001 to L2P-003 — AgentFactory broadcast (3 cases)
- [x] E2E-001 — End-to-end smoke (1 case)
- [x] Total: 16 cases across 5 test classes

## Risk Mitigation

- [x] R-01 broadcast registry leak — `@PreDestroy` deregister + JVM hook fallback (Risk table)
- [x] R-02 200ms CI flaky — CountDownLatch sync + 250ms margin + surefire fork
- [x] R-03 default method back-compat — no-op fire() preserves existing impls
- [x] R-04 shutdown hook repeat — `@PostConstruct` single trigger; idempotent empty broadcast
- [x] R-05 dep pollution — R-13 mandatory dep-tree check (T026)
- [x] R-06 token pollution — AtomicBoolean CAS + Tool cannot reverse-fire
- [x] R-07 LLM token waste — Story #005b planned; documented in plan.md "Future Scope"

## Definition of Done (Plan.md)

- [ ] All 9 implementation phases complete (T001—T031)
- [ ] PR opened with title `feat(agent): Story #005 cancellation-token — ...`
- [ ] PR body includes AC-04 wall-clock ≤ 200ms output + R-13 dep-tree section
- [ ] Critical invariants list all preserved (8 items)
- [ ] No new Maven dependency

## Anti-Patterns (from tasks.md)

- [x] Did NOT modify `CancellationToken.isCancelled()` / `onCancel()` signatures (back-compat preserved)
- [x] Did NOT use `Thread.interrupt()` (cooperative semantic via token polling)
- [x] Did NOT make `broadcastCancel()` async (sync per NFR-006)
- [x] Did NOT introduce `Reactor` / `RxJava` / `Guava` (JDK 8 only, R-13)
- [x] Did NOT use `var` / `List.of` / sealed interface (JDK 8 constraint)
- [x] Did NOT throw RuntimeException from `runTurn` on cancel (emit `TurnCompleted(CANCELLED, ...)` instead)
- [x] Did NOT include LlmProvider cancellation polling (deferred to Story #005b)
- [x] Did NOT include SessionStore flush on cancel (deferred to Story #014)
- [x] Did NOT include full graceful shutdown (deferred to Story #013)
- [x] Did NOT create new token per call in DefaultToolExecutionContext (shared identity per US2)

## Reviewer Sign-Off Checklist

When reviewing this Story's PR, verify:

1. **AC-04 black-box output** pasted into PR body — `wall-clock ≤ 200ms` + `RunResult.stopReason == CANCELLED`
2. **`mvn dependency:tree` diff** = 0 (compare with Story #004 baseline at `/tmp/deps-004-baseline.txt`)
3. **No regression** in Story #001—#004 tests (16 prior cases still green)
4. **Critical invariants** section complete + accurate (8 items)
5. **`CancellationToken` interface** still has `isCancelled()` + `onCancel(Runnable) → Runnable`; new `default void fire()` is back-compat
6. **`TurnContext` interface** has new `cancellation()` method; all impls (only `DefaultTurnContext` for now) implement it
7. **`DefaultTurnContext.createWithBroadcast()`** static factory exists + auto-registers with AgentFactory
8. **`AgentFactory.broadcastRegistry`** is `CopyOnWriteArrayList<CancellationToken>` (NFR-008)
9. **`AgentFactory.@PostConstruct registerJvmShutdownHook()`** registers `Thread("lingshu-shutdown-cancel")` via `Runtime.getRuntime().addShutdownHook(...)`
10. **`LinearTurnEngine.runTurn`** has 3 cancellation wiring points: loop head check (FR-006) + 200ms timeout fallback (FR-007) + dispatchParallel cancel poll (FR-008)
11. **`DefaultToolExecutionContext.cancellation()`** returns `turnCtx.cancellation()` (NOT a new anonymous no-op)
12. **No new ErrorCodes** (constitution §4 boundary)
13. **7 file changes** (1 new + 6 modified) — exceeds the soft 5-file cap by 2; verify the justification in spec.md / plan.md

---

**Checklist Generated**: 2026-09-20
**Story #005 Status**: Ready for implementation (Step 3 in progress)